"""
内嵌 PM 执行域 — 本机启停/恢复/注入、队列派发、子 Agent、孤儿迁移。

拆分自 station_controller.py (iter-74 Phase 1)。本 mixin 为空壳,
方法仍留在 StationController 类内, 待后续 Phase 4 搬入。
"""


class StationLocalPmMixin:
    """内嵌 PM 执行域 — 本机启停/恢复/注入、队列派发、子 Agent、孤儿迁移。

    Phase 1 空壳: 方法尚在 StationController 类内, 组合顺序已就位。
    """

    pass
