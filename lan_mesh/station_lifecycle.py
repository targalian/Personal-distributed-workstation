"""
进程生命周期与 Web 装配 — 端口选择、FastAPI 装配、WS 推流、dev-reload。

拆分自 station_controller.py (iter-74 Phase 1)。本 mixin 为空壳,
方法仍留在 StationController 类内 (类内定义优先于 mixin, 行为不变),
待后续 Phase 5 搬入。
"""


class StationLifecycleMixin:
    """进程生命周期与 Web 装配 — 端口选择、FastAPI 装配、WS 推流、dev-reload。

    Phase 1 空壳: 方法尚在 StationController 类内, 组合顺序已就位。
    """

    pass
