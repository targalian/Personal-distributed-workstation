- 后端命令函数统一采用 `state: State<'_, AppState>` 作为第一个参数，通过 Tauri 状态管理访问共享服务实例。
- 前端所有副作用操作（如发送文件、更新设置）均封装在 `runAction` 辅助函数中，以统一处理 `busy` 状态切换与错误提示。
- 网络数据包序列化统一使用 `serde_json`，并在 TCP 传输中采用 `[u32_length][json_payload]` 的帧结构以确保粘包处理。