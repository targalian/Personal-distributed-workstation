- **Tauri 2.0**: 使用 `staticlib`/`cdylib` 混合构建模式，集成 `tauri-plugin-dialog` 和 `opener`。
- **数据存储**: `rusqlite` (bundled) 用于持久化设备备注、共享索引及设备 ID。
- **网络库**: `tokio` 处理异步 TCP 文件流与 HTTP 控制接口；原生 `std::net::UdpSocket` 处理高频发现广播。