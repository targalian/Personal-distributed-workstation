- **角色化启动**：使用 `python main.py station` 启动管理端，`python main.py worker` 启动工作节点。
- **环境初始化**：运行 `scripts/setup_env.sh` (或 .bat/.ps1) 自动配置 Python 虚拟环境并安装依赖。
- **自定义配置**：支持通过 `--port`、`--name` 等命令行参数覆盖 `config.yaml` 中的默认设置。