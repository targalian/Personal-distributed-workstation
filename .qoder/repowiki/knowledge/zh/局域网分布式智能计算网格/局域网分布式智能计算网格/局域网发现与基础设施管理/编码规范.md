- 采用 dataclass 定义协议数据模型（如 DiscoveryPacket, HostRecord），并统一提供 to_dict/from_dict 方法以实现 JSON 序列化与反序列化。
- 在数据库操作中广泛使用 INSERT ... ON CONFLICT DO UPDATE 语句实现记录的幂等性更新，简化 CRUD 逻辑。
- 通过 threading.local 为每个线程分配独立的 SQLite 连接，确保多线程环境下的数据库访问安全。
- 在文件系统操作中执行严格的路径安全检查（resolve + startswith），防止相对路径遍历导致的安全漏洞。