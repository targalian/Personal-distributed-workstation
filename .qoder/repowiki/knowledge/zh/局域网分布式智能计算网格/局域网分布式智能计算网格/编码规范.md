- 控制器模式 (Controller Pattern)：每个核心角色（Worker, Secretary, Station）均封装为独立的 Controller 类管理生命周期。
- 状态共享对象 (State Object)：使用 dataclass 定义的 State 对象在控制器、API 路由与后台线程间共享运行时状态。
- 延迟绑定 (Lazy Binding)：基础设施组件（如 DiscoveryService）在启动阶段创建后，通过 bind 方法注入到业务逻辑组件中。