- 采用延迟绑定模式，在控制器启动阶段通过 `bind_discovery` 将网络发现服务注入到主管组件中。
- 所有涉及 Secretary 业务的 API 端点均通过检查 `controller.secretary_active` 标志位来决定返回业务数据或 503 状态码。
- 使用线程锁 `threading.Lock` 保护主机评级更新与数据库写入操作，确保多线程环境下的心跳处理一致性。
- 通过 `subprocess.Popen` 在 Worker 节点上以子进程形式动态启停 Secretary 服务，实现跨主机的角色灵活分配。