- 控制器类（如 StationController）持有核心组件引用，通过内部状态标志（如 secretary_active）控制 API 路由的可用性。
- 采用延迟绑定模式，将 DiscoveryService 等依赖在 start() 阶段注入到业务逻辑类（如 StationDirector）中。
- 所有 HTTP 接口均通过 FastAPI Router 模块化组织，并根据功能层级（基础/秘书）进行权限或状态检查。