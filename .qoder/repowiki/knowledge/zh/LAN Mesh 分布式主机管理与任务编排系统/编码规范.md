- 采用 Pydantic BaseModel 定义所有配置项与协议数据结构，确保数据校验与序列化的一致性。
- 在多线程环境下使用 threading.local 或 RLock 保证 SQLite 连接与共享状态（如设备列表）的线程安全。
- 遵循 Secretary/Worker 角色分离设计，通过统一的 `AppConfig` 加载差异化配置，并在启动时通过 `preflight` 进行环境自检。
- 使用 dataclass 配合 `to_dict` / `from_dict` 方法实现协议对象与 JSON 载荷之间的双向转换。