- **一键启动脚本**：提供跨平台脚本 (`scripts/start_workstation.{bat,ps1,sh}`) 自动完成环境检查、虚拟环境创建、依赖安装及服务启动。
- **角色化启动**：通过 `python main.py [station|worker|secretary]` 区分节点角色，Station 节点默认开启 Web UI (端口 45470)。
- **配置初始化**：需复制 `model_pool.example.yaml` 为 `model_pool.yaml` 并配置各 LLM Provider 的 API Key 环境变量。