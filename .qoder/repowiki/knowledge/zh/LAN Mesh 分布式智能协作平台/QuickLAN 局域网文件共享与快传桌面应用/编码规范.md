- 后端命令函数统一使用 `#[tauri::command]` 宏标记，并通过 `State<'_, AppState>` 注入共享状态。
- 前端 API 封装层 (`api.ts`) 将 `invoke` 调用包装为类型安全的 Promise 函数，保持与后端命令名一致。
- UI 组件采用原子化设计，通过 props 传递状态与回调，主逻辑集中在 `App.tsx` 的状态管理中。
- 网络协议消息与配置结构体均派生 `serde::Serialize` 和 `Deserialize`，确保前后端数据交换格式一致。