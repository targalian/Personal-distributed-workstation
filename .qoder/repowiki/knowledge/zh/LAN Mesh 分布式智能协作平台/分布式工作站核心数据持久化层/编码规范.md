- 所有涉及列表或字典的数据库字段（如 skills, subtasks, allowed_models）均序列化为 JSON 字符串存储，读取时统一反序列化。
- 采用 UPSERT 策略（INSERT ... ON CONFLICT DO UPDATE）处理主机、Agent 及任务的创建与更新，确保数据幂等性。
- 时间戳字段（如 last_seen, created_at）统一使用 Unix 时间戳（float）存储，便于跨平台计算与时区无关的比较。
- 查询方法返回 Pydantic 模型实例或字典，而写入方法直接执行 SQL 并提交事务，保持接口职责单一。