- **Web 框架**：使用 `FastAPI` 和 `uvicorn` 暴露 HTTP API 和 WebSocket 接口。
- **通信协议**：采用 UDP 广播进行局域网发现，HTTP/JSON 进行注册、心跳和任务分发。
- **LLM 集成**：通过 `requests` 直接调用兼容 OpenAI 格式的 LLM API，支持 DeepSeek、OpenAI、Anthropic 和 Qwen 等多提供商配置。