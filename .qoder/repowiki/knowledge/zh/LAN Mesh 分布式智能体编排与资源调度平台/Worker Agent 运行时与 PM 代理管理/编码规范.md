- 所有数据模型（如 HostInfo, AgentCard, Skill）均使用 `dataclass` 定义，并提供 `to_dict` 和 `from_dict` 方法以实现标准化的 JSON 序列化与反序列化。
- Agent 技能和工具的定义均采用 JSON Schema 格式描述输入参数，确保 LLM 调用和工具执行时的参数校验一致性。
- 模块内部通过依赖注入方式传递核心组件（如 `AgentRuntime`, `SharedFolderManager`），在创建 FastAPI 路由时通过 `create_worker_router` 函数注入状态和操作句柄。