- 采用 Pydantic BaseModel 定义所有配置类，确保配置项的类型安全与默认值管理。
- 数据库复杂对象字段（如 skills, subtasks, team_structure）统一序列化为 JSON 字符串存储，读取时反序列化。
- SQLite 连接采用 threading.local 实现线程隔离，并在初始化时通过 try-except 执行 ALTER TABLE 以兼容旧版本 schema。
- 路径处理统一使用 Path.expanduser() 和 resolve()，并在文件操作中增加防路径穿越（Path Traversal）的安全校验。