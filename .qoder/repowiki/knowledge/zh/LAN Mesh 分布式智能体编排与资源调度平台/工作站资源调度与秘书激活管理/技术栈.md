- **Web框架**：使用 `FastAPI` 构建高性能异步 HTTP API 和 WebSocket 服务。
- **数据存储**：采用 `SQLite` (`database.py`) 进行线程安全的主机记录、任务状态及 PM Agent 元数据持久化。
- **网络通信**：基于 `UDP` 广播 (`discovery.py`) 实现局域网设备自动发现，结合 `HTTP` (port 45460/45470) 进行注册与心跳保活。