- 采用延迟激活模式，Secretary 相关组件（如 ProjectManager, ChatHandler）仅在调用 `/api/station/activate-secretary` 后动态初始化，以降低基础模式下的资源占用。
- 统一使用 `to_dict()` 和 `from_dict()` 方法在 Dataclass 模型（如 `HostRecord`, `PMAgent`）与字典格式之间进行序列化转换，确保 API 载荷与数据库存储的一致性。
- 在 `StationDirector` 和 `Database` 中使用线程锁 (`threading.Lock` / `RLock`) 保护共享状态和 SQLite 连接，确保多线程环境下的数据一致性。
- 实行主机评级制度，根据 CPU、内存和磁盘资源自动计算 S/A/B/C/D 等级，并在 `StationDirector` 中提供按等级筛选在线主机的能力，用于 PM Agent 的智能选址。