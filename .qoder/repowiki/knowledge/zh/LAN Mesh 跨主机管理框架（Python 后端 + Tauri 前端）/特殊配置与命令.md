启动入口 `python main.py <role>`，role 可选 `station`（推荐）| `worker` | `resources`；配置文件可放 `~/.lan_mesh/config.yaml` 或项目根 `config.yaml`，也可通过 `--config` 或 `LAN_MESH_CONFIG` 环境变量覆盖；模型池通过 `model_pool.yaml` 或 `LAN_MESH_MODEL_POOL` 指定。