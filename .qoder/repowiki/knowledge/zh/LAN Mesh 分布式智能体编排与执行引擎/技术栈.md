- **LLM 路由与降级**：支持 DeepSeek/OpenAI/Anthropic/Qwen 等多厂商 API，通过 `model_router.py` 实现基于能力匹配度、成本反向指数和响应速度的加权评分路由，并支持失败降级链重试。
- **通信协议**：UDP 广播用于局域网设备发现 (`discovery.py`)，HTTP/FastAPI 用于任务分发与状态同步，WebSocket 用于实时推送。
- **配置管理**：基于 Pydantic 的强类型配置校验，支持 YAML 配置文件与环境变量注入。